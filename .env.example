# Server Port
PORT=3001

# Database (optional - runs in standalone mode if not set)
DATABASE_URL=""

# Redis (optional - runs without cache if not set)
REDIS_URL=""

# CORS Origins (comma-separated)
CORS_ORIGINS="http://localhost:3000,http://localhost:4001,https://gold-tracker-web.vercel.app"

# Web App (Development)
NEXT_PUBLIC_API_URL="http://localhost:3001/api"
NEXT_PUBLIC_SOCKET_URL="http://localhost:3001"

# Web App (Production - use these when deployed)
# NEXT_PUBLIC_API_URL="https://gold-tracker-api.onrender.com/api"
# NEXT_PUBLIC_SOCKET_URL="https://gold-tracker-api.onrender.com"
